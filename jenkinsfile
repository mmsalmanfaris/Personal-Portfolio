pipeline{
    agent any


    stages{
        stage('Version'){
            steps{
                    sh 'node --version'
            }
        }

        stage('Build'){
            steps{
                sh 'npm run build'
            }
        }
    }
}